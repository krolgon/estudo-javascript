//Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente.

const calculo = (base, expoente) => {
  console.log(Math.pow(base, expoente));
};

let teste = calculo(7, 2);
