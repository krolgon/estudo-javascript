const nome = ['ana', 'carolina'];
const sobrenome = ['goncalves', 'oliveira'];

const full = nome.concat(sobrenome);
console.log(full, nome, sobrenome);

console.log(['a', 'b'].concat([1, 2], [3, 4], 5, [[6, 7]]));
